Step 1: Make sure Python version is 3.9

Open CMD in the same folder as the project files

Step 2: Run these lines in order

python -m pip install --upgrade pip

pip install -r requirements.txt

pipwin install cairocffi

DONE!